{
  "name": "lantern-playwright",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo lint placeholder",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo test placeholder"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
